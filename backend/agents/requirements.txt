# ============================================================
# AI RCA ASSISTANT — Python Dependencies
# ============================================================
# Install all with: pip install -r requirements.txt
# ============================================================

# ------------------------------------------------------------
# Web Framework — FastAPI + server
# ------------------------------------------------------------
fastapi==0.111.0          # Our API framework
uvicorn[standard]==0.30.1 # Server that runs FastAPI
python-multipart==0.0.9   # Needed for file uploads

# ------------------------------------------------------------
# AI / LLM — LangChain ecosystem
# ------------------------------------------------------------
langchain==0.2.6                  # Core LangChain framework
langchain-core==0.2.10            # Base classes
langchain-community==0.2.6        # Community integrations
langgraph==0.1.9                  # Agent graph framework (the brain!)

# LLM Providers (install the one you use)
langchain-groq==0.1.6             # Groq (free LLaMA 3)
langchain-openai==0.1.14          # OpenAI + Azure OpenAI

# Embeddings
langchain-huggingface==0.0.3      # Free local embeddings
sentence-transformers==3.0.1      # Needed by HuggingFace embeddings

# ------------------------------------------------------------
# Vector Database — ChromaDB
# ------------------------------------------------------------
chromadb==0.5.3                   # Vector store for RAG

# ------------------------------------------------------------
# Database — PostgreSQL
# ------------------------------------------------------------
asyncpg==0.29.0                   # Async PostgreSQL driver
sqlalchemy[asyncio]==2.0.31       # ORM (Object Relational Mapper)
alembic==1.13.1                   # Database migrations

# ------------------------------------------------------------
# Data Validation — Pydantic (used by FastAPI)
# ------------------------------------------------------------
pydantic==2.7.4
pydantic-settings==2.3.4

# ------------------------------------------------------------
# Utilities
# ------------------------------------------------------------
python-dotenv==1.0.1              # Reads .env file
httpx==0.27.0                     # HTTP client (for testing)
tenacity==8.4.1                   # Retry logic (API calls)

# ------------------------------------------------------------
# Document Processing (for PDF/text uploads)
# ------------------------------------------------------------
pypdf==4.2.0                      # Read PDF files
python-docx==1.1.2                # Read Word documents (optional)

# ------------------------------------------------------------
# Testing
# ------------------------------------------------------------
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0                     # Also used as test client
